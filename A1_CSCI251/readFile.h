#ifndef READFILE_H
#define READFILE_H

#include <iostream>
#include <fstream>
#include <string>
#include <vector>

using namespace std;
void readFromFile(string filename);
vector<string> splitString(string input, string delimiter);
#endif
